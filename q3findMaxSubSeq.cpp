#include "header.hpp"
class Solution {
public:
    int maxPathLength(vector<vector<int>>& coordinates, int k) {
        auto coord = coordinates[k];
        sort(coord.begin(), coord.end());

        
    }
};